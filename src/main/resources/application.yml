spring:
  application:
    name: ehelpdesk
  jackson:
    serialization:
      indent-output: true
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mysql://localhost:3306/
    username: root
    password: root
    hikari:
      poolName: Hikari
      auto-commit: false

  jpa:
    database: MYSQL
    show-sql: true
  thymeleaf:
    cache: false